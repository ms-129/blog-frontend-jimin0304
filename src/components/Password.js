import Comment from "./Comment";

const Password = () => {

/*    return (
        <div>
            <input type="text" size="15" placeholder="password" value={comment.content} onChange={(updateComment)} onKeyPress={pressEnter}/>
        </div>
    );*/
};

export default Password;